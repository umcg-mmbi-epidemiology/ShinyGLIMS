# renv Configuration
# For reproducible R environment management

# Initialize renv for this project by running:
# renv::init()

# To take a snapshot of the current package library:
# renv::snapshot()

# To restore packages from the lockfile:
# renv::restore()
